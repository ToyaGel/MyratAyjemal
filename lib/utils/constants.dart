import 'package:flutter/material.dart';

const Color primaryColor = Color.fromRGBO(244, 227, 216, 1.0);
const Color dropdownColor = Color.fromRGBO(231, 205, 187, 1.0);
const Color titleTextColor = Color.fromRGBO(162, 126, 71, 1.0);
const Color textColor = Colors.black;
const Color dialogColor = Color.fromRGBO(29, 52, 53, 1.0);

ThemeData themeData = ThemeData(
  primaryColor: primaryColor,
  toggleableActiveColor: primaryColor,
  visualDensity: VisualDensity.adaptivePlatformDensity,
);
